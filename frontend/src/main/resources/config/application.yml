spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driverClassName: org.h2.Driver
  grpc:
    book:
      hostname: localhost
      port: 6565

---
spring:
  profiles: k8s
  datasource:
    url: jdbc:mysql://book.database/book
    username: user
    password: password
    driverClassName: com.mysql.jdbc.Driver
  grpc:
    book:
      hostname: localhost
      port: 6565
